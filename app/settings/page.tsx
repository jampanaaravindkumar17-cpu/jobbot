"use client";
/* eslint-disable react-hooks/set-state-in-effect */

import { FormEvent, useEffect, useState } from "react";
import { AppShell } from "../components/app-shell";
import { Icon } from "../components/icons";
import { PageIntro } from "../components/product-ui";

type Preferences = { name: string; headline: string; locations: string; privacy: boolean };
const defaults: Preferences = { name: "Aravind", headline: "Open to the right opportunity", locations: "India, Remote", privacy: true };

export default function SettingsPage() {
  const [preferences, setPreferences] = useState<Preferences>(defaults);
  const [saved, setSaved] = useState(false);
  useEffect(() => { try { setPreferences({ ...defaults, ...JSON.parse(window.localStorage.getItem("jobbot.preferences") ?? "{}") }); } catch { /* Keep defaults. */ } }, []);
  function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); window.localStorage.setItem("jobbot.preferences", JSON.stringify(preferences)); setSaved(true); }
  return <AppShell><div className="mx-auto max-w-5xl px-5 py-8 sm:px-8 lg:px-10"><PageIntro eyebrow="Settings" title="Shape your JobBot workspace." description="Manage the profile and preferences that guide your job discovery experience." />
    <form onSubmit={save} className="mt-8 grid gap-6 lg:grid-cols-[minmax(0,1fr)_280px]"><section className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-5 shadow-[var(--card-shadow)] sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[var(--brand-soft)] text-[var(--brand)]"><Icon name="user" className="h-5 w-5" /></span><div><h2 className="font-bold">Profile & preferences</h2><p className="text-xs text-[var(--text-muted)]">Used to personalize the workspace.</p></div></div><div className="mt-6 grid gap-5"><label><span className="text-sm font-semibold">Display name</span><input value={preferences.name} onChange={(event) => { setSaved(false); setPreferences({ ...preferences, name: event.target.value }); }} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--surface-subtle)] px-3.5 py-3 text-sm outline-none focus:border-[var(--brand)]" /></label><label><span className="text-sm font-semibold">Career headline</span><input value={preferences.headline} onChange={(event) => { setSaved(false); setPreferences({ ...preferences, headline: event.target.value }); }} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--surface-subtle)] px-3.5 py-3 text-sm outline-none focus:border-[var(--brand)]" /></label><label><span className="text-sm font-semibold">Preferred locations</span><input value={preferences.locations} onChange={(event) => { setSaved(false); setPreferences({ ...preferences, locations: event.target.value }); }} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--surface-subtle)] px-3.5 py-3 text-sm outline-none focus:border-[var(--brand)]" /></label></div><div className="mt-6 flex items-center justify-between rounded-xl border border-[var(--border)] p-4"><div><p className="text-sm font-semibold">Private profile</p><p className="mt-1 text-xs text-[var(--text-muted)]">Keep your personal preferences within JobBot.</p></div><button type="button" onClick={() => { setSaved(false); setPreferences({ ...preferences, privacy: !preferences.privacy }); }} className={`relative h-7 w-12 rounded-full ${preferences.privacy ? "bg-[var(--brand)]" : "bg-slate-400/40"}`} aria-label="Toggle private profile" aria-pressed={preferences.privacy}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition ${preferences.privacy ? "left-6" : "left-1"}`} /></button></div><button type="submit" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[var(--brand)] px-4 py-3 text-sm font-bold text-slate-950 hover:brightness-110"><Icon name="check" className="h-4 w-4" />{saved ? "Saved" : "Save preferences"}</button></section><aside className="h-fit rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-5 shadow-[var(--card-shadow)]"><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-[var(--brand)]">Appearance</p><h2 className="mt-1 font-bold">Theme follows your choice</h2><p className="mt-3 text-sm leading-6 text-[var(--text-muted)]">Use the sun or moon control in the top bar to switch between light and dark themes. Your choice is remembered on this device.</p></aside></form>
  </div></AppShell>;
}
